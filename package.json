{
  "name": "student-registration-tests",
  "version": "1.0.0",
  "description": "Automated test scripts for Student Registration Form (demoqa.com) - Midterm Exam",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.41.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.9.3"
  }
}
